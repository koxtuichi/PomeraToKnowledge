name: Story Auto-Publish Pipeline

on:
  repository_dispatch:
    types: [pomera-story]
  workflow_dispatch:
    inputs:
      draft_file:
        description: 'story_drafts/ 内のファイル名を指定して手動実行'
        required: false
        default: ''
        type: string

jobs:
  story:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests

      - name: Fetch Story Draft from Gmail
        if: ${{ github.event_name == 'repository_dispatch' }}
        env:
          GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}
          GMAIL_ACCOUNT: ${{ secrets.GMAIL_ACCOUNT }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
        run: |
          python scripts/sync_email.py --story-only

      - name: Generate Story and Publish
        env:
          GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}
          HATENA_ID: ${{ secrets.HATENA_ID }}
          HATENA_BLOG_ID: ${{ secrets.HATENA_BLOG_ID }}
          HATENA_API_KEY: ${{ secrets.HATENA_API_KEY }}
        run: |
          # story_drafts/ 内の未処理ファイルを処理
          if [ -d "story_drafts" ] && [ "$(ls -A story_drafts/*.txt 2>/dev/null)" ]; then
            mkdir -p story_drafts/processed
            for f in story_drafts/*.txt; do
              echo "📖 Processing: $f"
              python scripts/story_writer.py "$f" || echo "⚠️ 失敗: $f"
              # 処理済みの草案を移動して再処理を防止
              mv "$f" story_drafts/processed/ 2>/dev/null || true
              sleep 5
            done
          else
            echo "💤 処理対象の小説草案がありません"
          fi

      - name: Commit and Push changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "Antigravity Story Bot"
          git add story_drafts/ || true
          git add blog_ready/ || true
          git add blog_published/ || true
          git add sync_history.txt || true

          git diff --quiet && git diff --staged --quiet || (git commit -m "Auto-story: Fiction generated and published [skip ci]" && git push)
